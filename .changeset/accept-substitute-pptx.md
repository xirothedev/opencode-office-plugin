---
"@xirothedev/openoffice-plugin-opencode": patch
---

Fixed: `accept` no longer drifts on its existence check, `substitute` preserves trailing newlines, and PPTX suggestion approval targets the text box by snippet.
