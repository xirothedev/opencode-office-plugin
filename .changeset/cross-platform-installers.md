---
"@xirothedev/openoffice-plugin-opencode": minor
---

Cross-platform installers: `install.sh` (macOS/Linux/WSL/Git Bash) and `install.ps1` (Windows) set up the plugin and skills idempotently, preserving jsonc configs and `$schema`.
