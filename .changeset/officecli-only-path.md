---
"@xirothedev/openoffice-plugin-opencode": minor
---

**Breaking:** `officecli` is now the single path for office/PDF files — the builtin `read`, `edit`, and `write` tools are blocked for these extensions, and a tool-definition hint routes the agent to `officecli`.
