---
"@xirothedev/openoffice-plugin-opencode": minor
---

Every `officecli` invoke now writes a local JSON Capture (input, output, duration, error) to the plugin data dir — captures never leave the machine, no telemetry (ADR-0014).
