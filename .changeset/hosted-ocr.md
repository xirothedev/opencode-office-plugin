---
"@xirothedev/openoffice-plugin-opencode": minor
---

Hosted OCR for scanned PDFs: `read` accepts `ocr: true | "hosted" | "reject"` and routes image-only pages through anydoc, with a clear `needsOcr` error otherwise (ADR-0011).
