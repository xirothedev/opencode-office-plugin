---
"@xirothedev/openoffice-plugin-opencode": minor
---

Full comment lifecycle for DOCX/XLSX/PPTX: `edit-comment`, `delete-comment`, `resolve-comment`, and `deny-comment` join `comment`/`list-comments`/`approve`, and `approve` now refuses suggestions that were denied.
