---
"@xirothedev/openoffice-plugin-opencode": minor
---

Plugin API: `officecli` invokes are registered through `ctx.invoke`, and `office.preview` returns a structured result instead of a plain string.
